/**
 * Defines whether the entry is null or undefined
 * @param {any} obj The verification object
 * @return {boolean}
 */
module.exports = (obj) => {
    return obj === null || obj === undefined;
};
